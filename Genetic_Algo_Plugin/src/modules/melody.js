var LOWER_BOUND= 60;
var UPPER_BOUND = 70;

class Melody {
    

    constructor(tabs, fitnessScore){
        this.tabs = tabs;
        this.fitnessScore = fitnessScore;
        this.lower = lower
    }

}